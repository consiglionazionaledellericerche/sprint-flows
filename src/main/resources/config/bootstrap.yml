spring:
  application:
    name: flows
  cloud:
    config:
      server:
        bootstrap: true
        git:
          uri: git@git.si.cnr.it:devops/configuration-repo.git
          searchPaths: '{application}'
